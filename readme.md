# Contador de Cliques — React (Lab365)

Aplicação simples em **React** que implementa um **contador de cliques**.  
A cada clique no botão, o valor do contador é incrementado em **+1**, utilizando **estado (useState)** e **eventos (onClick)**.

> Projeto desenvolvido por **Daniel Fortes** para atividade do **Lab365**.

---

## ✅ Funcionalidades

- Exibe o valor atual do contador
- Botão para incrementar o contador a cada clique
- Interface simples e objetiva

---

## 🧪 Tecnologias utilizadas

- **React**
- **JavaScript**
- **CSS**

---

## 📁 Estrutura do projeto (resumo)

- `components/` → componentes reutilizáveis (ex.: `Contador.jsx`)
- `app/` → páginas/estrutura do app *(caso esteja usando Next.js)*
- `public/` → arquivos públicos (imagens, ícones etc.)

---

## ▶️ Como rodar o projeto localmente

### 1) Clonar o repositório
```bash
git clone https://github.com/devdanielfortes/contador-lab365.git
cd contador-lab365
